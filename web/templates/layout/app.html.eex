<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello Magpie!</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/bootstrap.css") %>">
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
        <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
        <script src="//ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"></script>
        <script src="<%= static_path(@conn, "/js/dygraph-1.0.combined.js") %>"></script>
  </head>

  <body>
  <nav class="navbar navbar-inverse">
    <div class="container-fluid">
      <div class="navbar-header">
      </div>
        <button type="button" class="btn btn-default navbar-btn navbar-left">Start</button>
        <%= if @current_user do %>
          <a href="/logout" type="button" class="btn btn-default navbar-btn navbar-right marginright marginleft">Logout</a>
          <a href="/users/<%= @current_user[:id] %>" type="button" class="btn btn-default navbar-btn navbar-right marginright"><%= @current_user[:username] %></a>
        <%= else %>
          <a href="/login" type="button" class="btn btn-default navbar-btn navbar-right marginright">Login</a>
        <% end %>
    </div>
  </nav>

<div class="container-fluid" role="main">

  <div class="row">
    <div class="col-sm-3 col-md-2 sidebar">
    <ul class="nav nav-sidebar">
      <li role="presentation"><a href="">Dashboard</a></li>
      <li role="presentation"><a href="">Loggere</a></li>
      <li role="presentation"><a href="">Book logger</a></li>
    </ul>
    </div>

    <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
      <%= if get_flash(@conn, :info) do %>
        <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <% end %>
      <%= if get_flash(@conn, :error) do %>
        <p class="alert alert-danger" role="alert"><%= get_flash(@conn,:error) %></p>
      <% end %>
      <%= @inner %>
    </div>

  </div>
</div> <!-- /container -->

    <%= render_existing view_module(@conn), "scripts." <> Phoenix.Controller.view_template(@conn), assigns %>

  </body>
</html>
